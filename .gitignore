.vercel
deploy/

# Local Claude Code config
.claude/

# WhatsApp data and proxy (sensitive)
whatsapp_messages.json
whatsapp_raw.json
whatsapp_proxy.py
proxy.log

# Internal AI / workflow assets
IMZA AI.pdf
IMZA_iMatch_n8n_Workflow.json

# OS
.DS_Store

# Backups
*.bak

# QuickLimes backend secrets — lives only on the Hostinger server, never committed
dashboard/api/config.php
dashboard/api/files/

# scratch MySQL datadir used by the PHP test suites — never committed
.scratch/

# local secrets — never commit
admin-config.local.js
